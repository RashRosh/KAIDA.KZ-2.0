# Проход 3 — результат ревью и обязательные правки

> **Кому:** агент-дизайнер, автор макета `KAIDA Pass 3`.
> **От:** Product Owner (RashRosh), 2026-09-23.
> **Основание:** `docs/product/WIREFRAME_TASK_PASS3.md` (ТЗ на этот заход) и проверка макета против фактического кода.
> **Статус макета:** принят по составу, **не принят** по одному экрану. Правки ниже обязательны, их объём — один экран и четыре мелочи.

## 0. Что принято и не переделывается

Это важнее списка правок: ниже перечислено то, что сделано верно. **Не трогай это, исправляя остальное.**

- Состав: 12 поверхностей, 63 состояния × `ru`/`kk`, 7 desktop-вариантов, разделы длинного контента, переходов и доступности. Закрытый список состояний из ТЗ §7.3 выполнен полностью.
- Казахский — настоящий, включая длинные строки в разделе длинного контента. Проверка длины сделана честно.
- Именование кадров по схеме и сопроводительная записка — по форме верны.
- Решение «экран ≠ состояние» проведено последовательно.
- Разбор старых кадров верен. В нумерации Tier 2 прав ты, а не спецификация: `3a` — Offer Workspace, `3b` — напоминание о свежести, `3c` — bulk-подтверждение. Спецификация исправлена по твоей записке.
- `S-05`: надпись «Не видно покупателям» у просроченного предложения — **фактически верна**, так работает код.
- `S-07__map-link` нарисован точно по утверждённому контракту: вставка ссылки, разбор, показ распознанного адреса. Этот кадр остаётся.

## 1. Блокирующая правка: экран торговой точки `S-07`

### Что не так

В `S-07__zero-create` поле адреса — это выпадающий список подсказок. В `S-07__address-confirm` — интерактивная карта с меткой. Обе механики требуют внешнего платного картографического сервиса, а он в KAIDA запрещён как обязательная зависимость: core-сценарии обязаны работать при полной недоступности любого внешнего SaaS.

Кроме того, из экрана **исчез существующий работающий путь**: действие «я на точке» через геолокацию браузера. Оно реализовано в продукте прямо сейчас. Макет его не показывает, а утверждённую вставку ссылки понижает до запасного пути — порядок оказался перевёрнут.

### Что сделать

Решение Product Owner: подсказки адреса остаются, но берутся из **собственного справочника KAIDA** на открытых данных (OpenStreetMap), размещённого внутри системы. Внешний геокодер не подключается. Интерактивная карта перестаёт быть обязательным шагом.

Целевой сценарий экрана:

1. продавец начинает вводить адрес;
2. KAIDA показывает варианты **из собственного справочника**;
3. выбор варианта даёт и адрес, и координаты — дополнительное подтверждение на карте не нужно, координаты уже известны;
4. если нужного адреса нет — видимый путь **«Ввести вручную»**;
5. для ручного адреса координаты задаются одним из двух способов: **«Я на точке»** (геолокация браузера, уже работает) или **вставка ссылки на карту** (утверждено контрактом, пока не реализовано).

Конкретно по кадрам:

| Кадр | Действие |
|---|---|
| `S-07__zero-create` | **Изменить.** Подсказки оставить. Добавить видимый, не спрятанный путь «Ввести вручную» рядом с полем адреса. Подпись под списком должна говорить, что подсказки — из справочника KAIDA, а не из внешнего сервиса |
| `S-07__address-confirm` | **Удалить из основной цепочки.** Подтверждение точки на карте не является шагом создания торговой точки |
| `S-07__manual-entry` | **Добавить новый кадр.** Ручной ввод адреса + два способа задать координаты: «Я на точке» и «Вставить ссылку на карту». Состояние «координаты ещё не заданы» должно быть видно честно |
| `S-07__on-site` | **Добавить новый кадр.** Действие «Я на точке»: объяснение до запроса разрешения, сам запрос, результат «координаты сохранены». Это существующий механизм, а не будущий |
| `S-07__map-link` | **Оставить как есть**, включая пометку «запасной путь вне основной F2». Механизм утверждён контрактом, но ещё не реализован — пометка верна |
| `S-07__one-selected`, `S-07__many-choose` | Без изменений |

Не рисуй интерактивную карту ни в одном из этих кадров. Если карта понадобится продукту позже, это будет отдельное решение с отдельным бюджетом.

## 2. Типы торговых точек: пять, а не два

В макете два типа — «Магазин» и «Место на рынке». В продукте их пять, и модель сужать не будем:

- Рынок
- Магазин
- Павильон
- Дом
- Другое

Нужны казахские названия для всех пяти.

На мобильном **не надо** пяти больших кнопок в ряд. Подойдёт `select` или лист снизу со списком типов — выбери то, что лучше ложится в композицию экрана, и покажи выбранный вариант.

## 3. Единица измерения: закрытый список плюс «Другое»

Сейчас в макете единица показана одним значением. Нужен явный выбор из закрытого списка:

- кг
- шт
- л
- упак.
- **другое** — при выборе появляется поле свободного ввода

Свободный ввод как основной механизм не используем: он быстро порождает `кг`, `килограмм`, `кг.`, `за кг` как разные значения одного и того же.

Отдельно: `100 г`, `500 г`, `1,5 кг` — **не единицы измерения**, а количество или размер упаковки. Не добавляй их в список.

## 4. Убрать таймер повторной отправки кода

В `S-03` есть надпись «Новый код — через 0:42». В продукте политики повторной отправки с обратным отсчётом нет: ни минимального интервала, ни ограничения числа попыток, ни состояния кнопки.

**Убрать надпись.** Когда такая политика появится, она проектируется целиком и отдельно.

## 5. Убрать код ошибки `5F2A`

Продукт не отдаёт таких идентификаторов — ошибки API имеют символьные коды вида `SEARCH_UNAVAILABLE`, которые пользователю не показываются. По выдуманному коду поддержка не найдёт ни одного запроса.

Текст ошибки содержит ровно две вещи: что не получилось и что можно сделать. Например:

> Не удалось сохранить. Проверьте соединение и повторите.

Без технического идентификатора. Это правило теперь записано в `DESIGN_SYSTEM.md` §7.1.

## 6. Ответы на твои восемь пробелов в данных

Пять закрываются фактами из кода — гадать больше не нужно.

| # | Твой вопрос | Ответ |
|---|---|---|
| 1 | Скрывается ли просроченное предложение из выдачи? | **Да, уже реализовано.** Покупателю видно только `active` с подтверждением свежее порога. Твоя надпись «Не видно покупателям» верна |
| 2 | Когда предложение становится просроченным? | **Через 168 часов (7 дней)** с последнего подтверждения. Порог задан в конфигурации. Не утверждён только график напоминаний — это отдельный будущий этап, и рисовать его не нужно |
| 3 | Откуда казахские названия товаров? | Сейчас негде: у товара одно поле названия. Решено, что каталог получит названия по языкам отдельным slice'ом (`Product → localized names → localized aliases`), с проверенными переводами, а **не** автопереводом на лету. В макете казахские названия товаров показывай, но в записке пометь, что их источник — будущая локализованная модель каталога |
| 4 | Реальные типы точек | Пять, см. раздел 2 |
| 5 | Реальный список единиц | Утверждён, см. раздел 3 |
| 6 | Есть ли код ошибки для поддержки | Нет, см. раздел 5 |
| 7 | Длительность кода и таймер повтора | Таймера нет, см. раздел 4. Срок жизни кода задаётся конфигурацией — конкретную цифру в макете не фиксируй |
| 8 | Округление расстояния в «Рядом» | Решения не требуется. Оставь округление как есть и пометь в записке как допущение |

## 7. Про автоперевод текста продавца

Твоё решение показывать текст продавца как автоперевод с пометкой и ссылкой «Показать оригинал» — принято и остаётся. Зафиксированы правила, которые макет должен соблюдать:

- оригинал продавца — всегда источник истины, продавец пишет текст один раз на своём языке;
- читатель всегда может открыть оригинал;
- если перевод не удался, показывается оригинал, и ничего не ломается;
- машинный перевод никогда не становится единственным сохранённым текстом.

Важное следствие для композиции: **состояние «перевод недоступен» должно быть предусмотрено**. Сейчас в макете его нет. Добавь его как вариант карточки в `S-01` — показывается оригинал с честной пометкой, без пустого места и без сообщения об ошибке.

## 8. Формат сдачи правок

Тот же, что и раньше: редактируемый макет с текстовым слоем, имена кадров по схеме `<ID>__<состояние>__<язык>__<размер>`, обе локали.

В записке к правкам достаточно четырёх разделов:

1. что изменено по пунктам 1–5 этого документа;
2. новые кадры `S-07__manual-entry` и `S-07__on-site` — как они встроены в цепочку F2;
3. оставшиеся допущения (в том числе округление расстояния и источник казахских названий товаров);
4. всё, где ты считаешь решение спорным.

Полную записку прохода 3 повторять не нужно — она принята.

## 9. Что будет после правок

Визуальная приёмка Product Owner по чек-листу `WIREFRAME_TASK_PASS3.md` §13. После неё — новая UI-ветка от актуального `main` и разбиение работы на компактные vertical slices. До приёмки продуктовые возможности из очереди не разрабатываются: действует feature freeze (`docs/product/EXECUTION_PLAN.md`).
